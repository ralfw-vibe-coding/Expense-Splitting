Der eingestellte Modus (hell, dunkel) soll gespeichert werden, um beim nächsten Start sofort aktiv
zu sein.

---

- Bereits umgesetzt: Theme wird in `localStorage` unter `theme` gespeichert und beim Start per
  `applyTheme(getStoredTheme())` angewendet.
